# Kaindet

## Run the backend locally

   ```powershell
   docker compose up -d
   ```

Run `BackendApplication` in IntelliJ, with the working directory set to the repository root or `backend/`, or run:

   ```powershell
   cd backend
   .\mvnw.cmd spring-boot:run
   ```
